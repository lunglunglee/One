@model IEnumerable<ONE.Models.Data_City>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Data_Province.ProvinceName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CityName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Data_Province.ProvinceName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CityName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CityCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.CityCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CityCode })
        </td>
    </tr>
}

</table>
